=== ANALISIS DATA PENILAIAN MANAJEMEN ===

🔍 CURRENT STATE ANALYSIS:

1. CONTROLLER LOGIC:
   ✅ Index method loads assignment submissions
   ✅ Index method loads practical submissions  
   ✅ Transform submissions to assessment objects
   ✅ Only shows ungraded submissions (whereNull('score'))
   ✅ Limits to 20 submissions each type

2. DATA SOURCES:
   ✅ AssignmentSubmission table
   ✅ PracticalSubmission table
   ✅ Filtered by guru_id
   ✅ Only ungraded items

3. VIEW EXPECTATIONS:
   ✅ Shows $assessments collection
   ✅ Statistics cards (total, graded, ungraded, average)
   ✅ Table with student info, activity, score, status
   ✅ Empty state when no data

📱 POTENTIAL ISSUES:

1. NO SUBMISSIONS:
   ❌ No students have submitted assignments
   ❌ No students have submitted practicals
   ❌ All submissions already graded

2. DATA FILTERING:
   ❌ Guru filter too restrictive
   ❌ whereNull('score') excludes graded items
   ❌ Class/subject relationship issues

3. TRANSFORMATION ISSUES:
   ❌ Student relationship not loaded
   ❌ Subject relationship missing
   ❌ Class relationship problems

🎯 RECOMMENDED DEBUGGING:

1. ADD DEBUG INFO TO CONTROLLER:
```php
// Add after line 180
\Log::info('Penilaian Debug', [
    'assignment_submissions_count' => $assignmentSubmissions->count(),
    'practical_submissions_count' => $practicalSubmissions->count(),
    'total_assessments' => $assessments->count(),
    'guru_id' => $guruId,
    'has_assignment_submissions_table' => \Schema::hasTable('assignment_submissions'),
    'has_practical_submissions_table' => \Schema::hasTable('practical_submissions'),
    'total_assignment_submissions_in_db' => \DB::table('assignment_submissions')->count(),
    'total_practical_submissions_in_db' => \DB::table('practical_submissions')->count(),
]);
```

2. ADD DEBUG INFO TO VIEW:
```blade
<!-- Add after statistics cards -->
@if(request()->has('debug'))
<div class="alert alert-info mb-4">
    <h6>Debug Information:</h6>
    <ul class="mb-0 text-sm">
        <li>Total Assessments: {{ $assessments->count() }}</li>
        <li>Assignment Submissions: {{ $assignmentSubmissions->count() ?? 'N/A' }}</li>
        <li>Practical Submissions: {{ $practicalSubmissions->count() ?? 'N/A' }}</li>
        <li>Guru ID: {{ Auth::id() }}</li>
    </ul>
</div>
@endif
```

3. CHECK DATABASE DIRECTLY:
```sql
-- Check assignment submissions
SELECT COUNT(*) as total_assignments FROM assignment_submissions;

-- Check practical submissions  
SELECT COUNT(*) as total_practicals FROM practical_submissions;

-- Check ungraded submissions
SELECT COUNT(*) as ungraded_assignments 
FROM assignment_submissions 
WHERE score IS NULL;

-- Check guru's submissions
SELECT COUNT(*) as guru_assignments 
FROM assignment_submissions s
JOIN assignments a ON s.assignment_id = a.id 
WHERE a.guru_id = [CURRENT_GURU_ID];
```

🔧 QUICK FIXES:

1. SHOW ALL SUBMISSIONS (including graded):
```php
// Remove whereNull('score') filter
$assignmentSubmissions = AssignmentSubmission::with(['siswa.kelas', 'assignment.subject'])
    ->whereHas('assignment', function($query) use ($guruId) {
        $query->where('guru_id', $guruId);
    })
    ->latest('submitted_at')
    ->limit(20)
    ->get();
```

2. ADD MANUAL ASSESSMENTS:
```php
// Include manually created assessments
$manualAssessments = \App\Models\Assessment::where('guru_id', $guruId)
    ->with(['student.kelas', 'subject'])
    ->latest()
    ->limit(20)
    ->get();

// Merge with submissions
$allAssessments = $assessments->merge($manualAssessments);
```

3. IMPROVE EMPTY STATE:
```blade
@empty($assessments)
<tr>
    <td colspan="10" class="text-center py-5">
        <div class="empty-state">
            <div class="empty-state-icon mb-3">
                <i class="fas fa-clipboard-list text-muted" style="font-size: 4rem;"></i>
            </div>
            <h5 class="text-muted mb-3">Belum Ada Penilaian</h5>
            <p class="text-muted mb-4">
                Belum ada data penilaian yang tersedia. 
                Ini bisa karena:<br>
                • Siswa belum mengumpulkan tugas/praktikum<br>
                • Semua penilaian sudah dinilai<br>
                • Anda belum membuat penilaian manual
            </p>
            <div class="d-flex gap-2 justify-content-center">
                <a href="{{ route('guru.penilaian.create') }}" class="btn btn-primary">
                    <i class="fas fa-plus me-2"></i>Buat Penilaian Manual
                </a>
                <a href="{{ route('guru.assignments.index') }}" class="btn btn-outline-primary">
                    <i class="fas fa-tasks me-2"></i>Lihat Tugas
                </a>
                <a href="{{ route('guru.practicals.index') }}" class="btn btn-outline-primary">
                    <i class="fas fa-flask me-2"></i>Lihat Praktikum
                </a>
            </div>
        </div>
    </td>
</tr>
@endempty
```

🎉 NEXT STEPS:

1. ADD DEBUG INFO to see actual data counts
2. CHECK DATABASE for existing submissions
3. VERIFY GURU ID matches assignments
4. CONSIDER SHOWING ALL SUBMISSIONS (not just ungraded)
5. IMPROVE EMPTY STATE with helpful actions

🔍 MOST LIKELY CAUSE:
Tidak ada submission yang tersedia karena:
- Siswa belum mengumpulkan tugas/praktikum
- Semua submission sudah dinilai (score not null)
- Guru filter tidak match dengan assignment creator
- Tidak ada manual assessments yang dibuat
